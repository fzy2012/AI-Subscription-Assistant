# AI 订阅助手 (sub.ruhang365.cn) 项目全局规则与上下文

这是一个独立的 Web 应用项目，主要职能是“AI 订阅省钱与避坑”。
当前项目属于“入行 365”生态的独立服务站点。

## 技术栈与规范
- 核心：**Vite + React** (视实际配置而定)
- 样式：**Tailwind CSS**，尽量使用 utility classes。
- **美学要求**：面向 AI 用户群，保持与主站一致的极简、暗黑或极客风，聚焦于订阅工具的实用性。

## 代码提交流程
由于开发者可能在本地直接使用 Cursor/Codex 和 Antigravity 进行双端开发，请在开始执行大批量修改前，确保拉取最新的 Git 记录。
在每次完成阶段性工作后，务必将修改 commit 并 push 到 `main` 或主分支。

## 与原生 Agent (Antigravity) 的协作协议 (Dual-AI Protocol)
当前开发者采取 "Codex + Antigravity" 双引擎协同工作流。
为了保证协作顺滑，请 Codex（你）在编写代码时务必遵守以下准则：
1. **优先读取 Git Log**：每次开启新任务时，你应该意识到代码库可能刚刚被后端的 Antigravity 修改过。如果有疑问，请先查看最新的 Git Commits。
2. **保持文件原子性**：在修改复杂的业务逻辑时，尽量将功能拆分为独立的小组件，避免与其他 AI 并发修改同一个大文件。
3. **架构遵从**：你负责该独立站的前端 UI 与业务功能。跨项目的环境配置、构建流和部署运维由 Antigravity 在终端层面处理，请勿随意破坏 `vite.config.js` 等全局构建配置。
4. **交接留言**：如果你完成了一个大模块，请确保你的代码语义清晰，或者在关键文件的头部留下简短的注释，方便后置接手的 Antigravity 能够瞬间读懂你的设计意图。
